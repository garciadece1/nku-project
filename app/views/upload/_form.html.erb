<%= form_tag({action: :import}, multipart: true) do %>
  <div class="form-group"><%= file_field_tag :file, accept: "*" %></div>
  <div class="form-group"><%= submit_tag "Upload", class: "btn btn-primary" %></div>
<% end %>
